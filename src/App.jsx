import { HomePage } from './pages/HomePage';

/**
 * App root.
 * When React Router is added for multi-page navigation,
 * replace this with <RouterProvider router={router} />.
 */
export default function App() {
  return <HomePage />;
}
